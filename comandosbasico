#para criar uma variável no JS
  #let xxx = yyy
  #const xxx = yyy (criar constantes)
#para criar uma função
  funcion xxx() = {yyy}

#efeitos visuais
  #background() = criar uma cor de fundo
  #width = limites do framework
  
#comandos básicos
  #para usar mais de 1 argumento em uma função: "||" (duas barras verticais)
  
#funções básicas
  #noLoop() = interromper a função
  #reDraw() = rodas o código 1 vez
  #Loop() = rodar o código continuamente
  
#primeiros comandos
  #toLowerCase() = transforma a variável em minúscula.
  #lenght() = tamanho em caracteres da variável.
  
